import '../components/css/tienda.css'
import MenuFroend from './MenuFroend'

import React from 'react'
//Por problemas con el css desmantelo el componente en otro para evitar que utilice el que tiene por default

const EstiloMenu = () => {
  return (
    <MenuFroend/>
  )
}

//Exportamos el componente
export default EstiloMenu